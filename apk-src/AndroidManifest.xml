<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.qflash21.app"
    android:versionCode="50"
    android:versionName="2.0.0">

  <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34"/>

  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

  <!-- USB-Host: D+KCAN-Kabel (FTDI/CH340/CP2102) direkt per Android USB-Host-API.
       required=false: App bleibt auf Geräten ohne USB-Host installierbar. -->
  <uses-feature android:name="android.hardware.usb.host" android:required="false"/>

  <application
      android:name=".QfApp"
      android:label="QFLASH21"
      android:icon="@mipmap/ic_launcher"
      android:allowBackup="false"
      android:supportsRtl="true"
      android:usesCleartextTraffic="false"
      android:theme="@android:style/Theme.Material.NoActionBar">

    <!--
      v2.0.0 – ECHTE NATIVE APP (ersetzt die TWA-Architektur v1.x):
      - WebView lädt die QFLASH21-Weboberfläche
      - SerialBridge (window.QfSerialBridge) exponiert die Android USB-Host-API
        → FTDI FT232R/FT231X, CH340, CP2102 NATIV, ohne Chrome / Web Serial / TWA
      - Kein Custom-Tabs-, kein assetlinks-, kein Chrome-Version-Abhängigkeit mehr
      - Gleiche Paket-ID + Signatur wie v1.3.1 → sauberes Upgrade
      - QfApp: globaler Crash-Handler → nativer deutscher Fehlerbericht
    -->
    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode|density">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="https" android:host="qflashk.vercel.app"/>
      </intent-filter>
    </activity>

    <!-- Nativer deutscher Fehlerbericht (auch ohne WebView lauffähig) -->
    <activity
        android:name=".CrashActivity"
        android:exported="false"
        android:excludeFromRecents="true"/>
  </application>
</manifest>
